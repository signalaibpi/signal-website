# UI

<!-- Document visual facts that carry meaning (e.g. a status badge's
     color-to-value mapping) since getting those wrong is a real behavioral
     regression a user would notice. Don't restate raw styling constants with
     no independent meaning — hex colors, pixel sizes, spacing, border
     radius. Those have one source of truth in the codebase's design
     system/theme already; repeating them here creates a second one that
     drifts, and inflates "docs change with behavior" to cover cosmetic
     tweaks no one needs to track. See Templates/README.md → Design
     principle. -->

Executive, precise, restrained — a light canvas with a blue accent used sparingly for primary actions and diagnostic priorities, generous whitespace, and a strict grid with softened corners. Built with Next.js, TypeScript and Tailwind CSS (see `TECHNICAL.md`).

Visual design source of truth: `/Users/erikdekroon/Documents/Signal/Website/Signal Website Design Guidelines.md` (and `Gemini Image Style Guidelines.md` for imagery). Follow the design-doc split above — don't restate raw styling constants (colors, spacing, etc.) from those guidelines here; only document a visual fact once it carries behavioral meaning for a specific page.

When UI behavior, placement, visible labels, or page structure changes, update this document in the same change set.

## Authentication
There is no login. The site is fully public.

## Pages

## Homepage

Route: `/`

### Purpose
- Explain what Signal does and why it's different (frontline-centred AI transformation).
- Introduce the Business AI Diagnostic and Signal's other two services (AI Strategy Planning, Embedded AI Delivery).
- Build credibility (founder background) and give visitors a clear next step.

### Sections (in order)
1. **Hero** — headline, subhead, primary CTA ("Book a free AI Snapshot") and secondary CTA ("View a sample diagnostic"), plus an illustrative Business AI Diagnostic sample card.
2. **Our approach** — frontline-centred AI transformation explanation.
3. **Benefits** — three cards: prioritise, find what's holding progress back, scale what's working.
4. **Business AI Diagnostic** — three cards, each tagged with the diagnostic dimension range it covers (e.g. "Diagnostic dimensions 01–04").
5. **Other services** — AI Strategy Planning and Embedded AI Delivery modules.
6. **Who is this for** — audience statement plus sector tags (Software, Professional Services, Distribution, Outsourcing — exactly the four sectors named in the approved copy, no others).
7. **Founder** — Erik de Kroon's background.
8. **How it works** — four numbered engagement steps.
9. **Final CTA** — repeats both hero CTAs.

There is no separate "Problem" section — `Signal Website Design Guidelines.md`'s homepage section sequence calls for one between Hero and Our Approach, but `Signal Website Copy.md` has no copy for it. Erik confirmed (2026-09-18) that Benefits, one section later, covers that narrative role instead; the Design Guidelines doc hasn't been updated to match.

Nav and footer links other than the homepage itself point to `#` — those pages don't exist yet and will be wired up as they're built.

### Visual facts that carry meaning
- The hero's Business AI Diagnostic card shows all 12 real dimensions as a grid of numbered tiles (number + name, no scores). Each tile is tinted by which of the diagnostic's 4 areas the dimension belongs to (Strategy/Implementation/Operation/Governance — see `Signal Business AI Diagnostic.md` → Areas), with the area name in small caps top-right of the tile. This intentionally departs from Design Guidelines' "use neutral tones, accent reserved for a selected dimension/priority" rule — Erik decided area color-coding was worth it over an all-grey grid; a first neutral version is preserved in git history (`components/home/Hero.tsx` as of commit `119b028`) if the guideline's stricter reading is ever preferred instead. The 4 area colors (`app/globals.css` → `--color-area-*`) are the same ones planned for the future Diagnostic page's radial chart — see `Website/Business AI Diagnostic page/Visual ideas.md`. Green/amber/yellow are deliberately excluded from all 4 area colors — those are reserved by the diagnostic's own RAG status system (`Signal Business AI Diagnostic.md` → Output) and reusing them here would collide with that meaning. Dimension names and area assignments must always match the real 12 dimensions (see `Signal Business Plan.md` → Business AI Diagnostic).
- The hero has no image — the 12-dimension card is the only hero visual, top-aligned with the headline column. It originally sat layered over a placeholder photo, but once the card grew to show all 12 dimensions it was taller than that pairing could support without leaving a dead gap below the CTAs; removing the (still-unsourced) placeholder photo fixed the imbalance directly rather than just repositioning the card. Photography still appears elsewhere on the page (Our Approach, Who is this for, How it works).

### States
- No loading, error, or empty states — the page is fully static with no data fetching.

### Acceptance Tests
_None yet — no automated tests for this project (see `TECHNICAL.md` → Testing & CI/CD)._

